Django==4.2.13
django-crispy-forms==2.1
crispy-bootstrap5==0.7
Pillow==10.3.0
python-decouple==3.8
whitenoise==6.6.0
django-extensions==3.2.3

# SNMP connectivity (pure-Python, no native deps)
# pysnmp 6.x is the current maintained package (lextudio changes merged back in)
pysnmp

# Task queue (background polling for 1000+ OLTs)
celery[redis]==5.3.6
redis==5.0.3
django-celery-beat==2.6.0
django-celery-results==2.5.1
